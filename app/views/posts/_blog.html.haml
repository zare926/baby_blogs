.main_content
  .main_content-box
    .article-list
      - @posts.each do |p|
        .article.b-article.fadein
          - if p.image.present?
            %figre.article-image
              = link_to (image_tag p.image.url,class:'image'),post_path(p.id)
          - else
            %figre.article-image
              = link_to image_tag(asset_path('noimage.jpeg'), class:'image cover'),post_path(p.id)
              .image_box NonImage
          .article_text-box
            .article_text-box-top
              %p.article_logo BLOG
              %a{href:post_path(p.id),class:'article_title'}
                .article_title-b
                  = p.title
                %p.article_time
                  = p.created_at.strftime("%Y.%m.%d %H:%M")
                %p.article_summary
                  = simple_format(p.text.truncate(150,omission: '...'))
            .article_text-box-bottom
      = paginate @posts